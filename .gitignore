# Local configuration files (machine-specific, not tracked)
*.local
.zshrc.local
.vimrc.local
.gitconfig

# Backup files created by install script
*.pre.install

# macOS
.DS_Store

# Editor files
*.swp
*.swo
*~

# SSH private keys and config (NEVER commit sensitive SSH files)
.ssh/id_*
.ssh/*.pem
!.ssh/*.pub
.ssh/config
.ssh/authorized_keys

# GPG keys
*.gpg
*.asc
secring.*

